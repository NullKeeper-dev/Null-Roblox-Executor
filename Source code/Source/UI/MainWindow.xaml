<Window x:Class="Null.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
        Title="Null Executor" Height="450" Width="800"
        Background="#1A0F2E"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True">
    <Window.Resources>
        <ResourceDictionary>
            <!-- Dark Theme Colors -->
            <Color x:Key="BackgroundDark">#0F0F0F</Color>
            <Color x:Key="SurfaceDark">#1A1A1A</Color>
            <Color x:Key="PrimaryAccent">#3F3F3F</Color>
            <Color x:Key="TextWhite">#FFFFFF</Color>
            <Color x:Key="TextGray">#AAAAAA</Color>
            <Color x:Key="ButtonHover">#2A2A2A</Color>
            <Color x:Key="ButtonPressed">#3A3A3A</Color>
            
            <SolidColorBrush x:Key="BackgroundDarkBrush" Color="{StaticResource BackgroundDark}"/>
            <SolidColorBrush x:Key="SurfaceDarkBrush" Color="{StaticResource SurfaceDark}"/>
            <SolidColorBrush x:Key="PrimaryAccentBrush" Color="{StaticResource PrimaryAccent}"/>
            <SolidColorBrush x:Key="TextWhiteBrush" Color="{StaticResource TextWhite}"/>
            <SolidColorBrush x:Key="TextGrayBrush" Color="{StaticResource TextGray}"/>

            <Style TargetType="{x:Type ScrollBar}">
                <Setter Property="Background" Value="#1A1A1A"/>
                <Setter Property="BorderBrush" Value="#2A2A2A"/>
                <Setter Property="Width" Value="12"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid x:Name="Bg" Background="#1A1A1A" SnapsToDevicePixels="true">
                                <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsMouseOver}">
                                    <Track.Thumb>
                                        <Thumb>
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="{x:Type Thumb}">
                                                    <Border Background="#3F3F3F" CornerRadius="2" Margin="2"/>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="Orientation" Value="Horizontal">
                        <Setter Property="Width" Value="Auto"/>
                        <Setter Property="Height" Value="12"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollBar}">
                                    <Grid x:Name="Bg" Background="#1A1A1A" SnapsToDevicePixels="true">
                                        <Track x:Name="PART_Track" IsEnabled="{TemplateBinding IsMouseOver}">
                                            <Track.Thumb>
                                                <Thumb>
                                                    <Thumb.Template>
                                                        <ControlTemplate TargetType="{x:Type Thumb}">
                                                            <Border Background="#3F3F3F" CornerRadius="2" Margin="2"/>
                                                        </ControlTemplate>
                                                    </Thumb.Template>
                                                </Thumb>
                                            </Track.Thumb>
                                        </Track>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- ScrollViewer style to fix the white corner -->
            <Style TargetType="{x:Type ScrollViewer}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollViewer}">
                            <Grid Background="{TemplateBinding Background}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <ScrollContentPresenter Grid.Column="0" Grid.Row="0" Margin="{TemplateBinding Padding}" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}"/>
                                <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="1" Grid.Row="0" AutomationProperties.AutomationId="VerticalScrollBar" Cursor="Arrow" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportHeight}"/>
                                <ScrollBar x:Name="PART_HorizontalScrollBar" Grid.Column="0" Grid.Row="1" AutomationProperties.AutomationId="HorizontalScrollBar" Cursor="Arrow" Orientation="Horizontal" Maximum="{TemplateBinding ScrollableWidth}" Minimum="0" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportWidth}"/>
                                <!-- This is the corner piece -->
                                <Rectangle Grid.Column="1" Grid.Row="1" Fill="#1A1A1A"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ModernButton" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource SurfaceDarkBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextWhiteBrush}"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="Margin" Value="5"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryAccentBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#2A2A2A"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#3A3A3A"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Background="{StaticResource BackgroundDarkBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceDarkBrush}" MouseLeftButtonDown="TitleBar_MouseDown" BorderBrush="{StaticResource PrimaryAccentBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <TextBlock Text="Null Executor" Foreground="{StaticResource TextWhiteBrush}" VerticalAlignment="Center" Margin="20,0" FontWeight="Bold" FontSize="24"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="—" Style="{StaticResource ModernButton}" Width="40" Click="Minimize_Click" Background="Transparent" BorderThickness="0" FontSize="18"/>
                    <Button Content="✕" Style="{StaticResource ModernButton}" Width="40" Click="Close_Click" Background="Transparent" BorderThickness="0" FontSize="18"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="160"/>
            </Grid.ColumnDefinitions>

            <!-- Script Editor -->
            <Border Grid.Column="0" Margin="10" BorderBrush="{StaticResource PrimaryAccentBrush}" BorderThickness="1">
                <avalonEdit:TextEditor Name="ScriptEditor" 
                                     FontFamily="Consolas" 
                                     FontSize="11pt" 
                                     Background="#0A0A0A" 
                                     Foreground="White"
                                     ShowLineNumbers="True"/>
            </Border>

            <!-- Sidebar Buttons -->
            <StackPanel Grid.Column="1" Margin="5,10">
                <Button Content="Execute" Style="{StaticResource ModernButton}" Height="35" Click="Execute_Click"/>
                <Button Content="Open File" Style="{StaticResource ModernButton}" Height="35" Click="OpenFile_Click"/>
                <Button Content="Save Script" Style="{StaticResource ModernButton}" Height="35" Click="SaveScript_Click"/>
                <Button Content="Clear" Style="{StaticResource ModernButton}" Height="35" Click="Clear_Click"/>
                <Separator Height="10" Background="Transparent"/>
                <Button Content="Attach" Style="{StaticResource ModernButton}" Height="45" Click="Attach_Click" FontWeight="Bold" BorderBrush="#444"/>
            </StackPanel>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceDarkBrush}" BorderBrush="{StaticResource PrimaryAccentBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                <TextBlock Text="Status: " Foreground="{StaticResource TextGrayBrush}" FontSize="11"/>
                <TextBlock Name="StatusText" Text="Not Attached" Foreground="#CC4444" FontWeight="SemiBold" FontSize="11"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
