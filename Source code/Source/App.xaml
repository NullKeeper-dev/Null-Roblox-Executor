<Application x:Class="Null.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Null">
    <Application.Resources>
         <ResourceDictionary>
            <Color x:Key="PrimaryPurple">#2D1B4E</Color>
            <Color x:Key="SecondaryPurple">#1A0F2E</Color>
            <Color x:Key="AccentPurple">#7B4BFF</Color>
            <Color x:Key="TextWhite">#FFFFFF</Color>
            
            <SolidColorBrush x:Key="PrimaryPurpleBrush" Color="{StaticResource PrimaryPurple}"/>
            <SolidColorBrush x:Key="SecondaryPurpleBrush" Color="{StaticResource SecondaryPurple}"/>
            <SolidColorBrush x:Key="AccentPurpleBrush" Color="{StaticResource AccentPurple}"/>
            <SolidColorBrush x:Key="TextWhiteBrush" Color="{StaticResource TextWhite}"/>

            <Style x:Key="ModernButton" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource AccentPurpleBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource TextWhiteBrush}"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="Margin" Value="5"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
